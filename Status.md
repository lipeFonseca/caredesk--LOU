# Status do Projeto CareDesk

Atualizado em: 2026-07-23

Este documento tem uma regra única de organização: **ordem cronológica estrita**. A primeira seção é uma visão geral fixa (o que o sistema é, sem data — não muda de posição). Depois disso, tudo é linha do tempo: mais antigo no topo, mais recente sempre na última entrada do arquivo. Nada de seção "estado atual" separada com data própria disputando com o changelog — pra saber o comportamento de hoje de qualquer parte do sistema, a resposta é sempre a entrada mais recente daquele assunto na linha do tempo.

---

## Visão Geral (sem data — descreve o que o projeto é, não muda de lugar)

CareDesk é um sistema interno de acompanhamento pós-operatório de uma clínica. Um agente (ou o admin) cadastra o paciente, vincula um protocolo de contato, e o sistema calcula os marcos de acompanhamento. O scheduler diário gera notificação interna quando um marco vence.

**Escala do produto:** uma clínica, dois papéis fixos — `agent` (cadastro, contato, checklist de documento) e `admin` (tudo isso + protocolos, equipe, identidade visual). Multi-clínica foi avaliado e descartado (ver `2026-07-20`).

**Stack:** React 18 + Vite + Tailwind + Framer Motion (frontend) · Cloudflare Workers + Hono (backend) · Cloudflare D1/SQLite (banco) · Cloudflare R2 (arquivo) · JWT hand-rolled (Web Crypto, sem lib) + cookie `HttpOnly` (sessão).

**Ambientes:**
- Frontend: `https://caredesk-lou.pages.dev`
- Worker: `https://caredesk-worker.faugusto-thecoral.workers.dev`
- Local: frontend `http://localhost:5173` (proxy `/api` → `http://localhost:8787`), worker `http://localhost:8787`
- **D1 local e D1 remoto são bancos separados** — dado, credencial e migration aplicada num não existem automaticamente no outro (ver armadilha em `2026-07-22`).

**Regra de storage:** R2 pra binário grande (logo, background, avatar), D1 pra metadado/relação/permissão. Nunca usar R2 como fonte de dado cadastral/clínico.

---

## Linha do Tempo

### 2026-07-11

- **Avatares de agente + storage organizado.** Primeira aplicação da regra R2/D1. Rotas `POST/DELETE /api/agents/:id/avatar`, `GET /api/agents/avatar/:key`. Migration `0002_agent-avatars.sql`. Sidebar e lista de equipe passam a renderizar avatar real com fallback por iniciais.
- **Imagem exclusiva de login.** Nova chave `login_image_url`, independente da imagem de fundo geral do painel. Migration `0003_login-branding.sql`.
- **Correção de favicon.** Não aplicava em todas as variações (`icon`/`shortcut icon`/`apple-touch-icon`) — `frontend/index.html` ganhou favicon base inline, runtime passou a atualizar as três tags.
- **Especificação da borda pulsante do login** (decisão de design, ainda não implementada): shader `PulsingBorder`, aplicado só no card de login, configurações enxutas na aba de Identidade Visual (preset, cores, intensidade, velocidade, espessura, bloom). Biblioteca `@paper-design/shaders-react` validada como viável.
- **Implementação inicial da borda pulsante.** Configurações persistidas em `app_settings` (migration `0004_login-border-settings.sql`). Import estático da lib no bundle principal (decisão revisitada depois, ver `2026-07-14`).
- **Deploy publicado:** migrations `0002`/`0003` aplicadas no remoto, `/health` `200`.

### 2026-07-12

- **Refino da borda pulsante:** glow passou a envolver o card principal inteiro (antes só a coluna direita); glass suave restrito à coluna direita (credenciais); coluna institucional da esquerda mantida sólida.
- **Ajuste do fluxo oficial de publicação:** workflow `.github/workflows/deploy.yml` renomeado pra "Deploy CareDesk", `run-name` mais legível, concorrência ajustada pra não cancelar run anterior.
- **Regressão real no login premium, causa raiz explicada:** o visual tinha sido publicado manualmente no Cloudflare; um push menor logo depois acionou o GitHub Actions, que republicou o repositório — que ainda não tinha o pacote visual completo — e a produção "voltou" pra um estado anterior. **Regra endurecida a partir daqui: nenhuma mudança visual crítica pode ficar só no deploy manual; o estado canônico mora no GitHub antes do próximo deploy oficial.**
- **Alinhamento de versionamento:** rodada dedicada a trazer pro GitHub o que já estava consolidado só localmente, evitando a repetição do problema acima.
- **Correção de escopo da borda pulsante e simplificação da coluna direita:** ajustes visuais pra bater com a decisão consolidada (glow no card inteiro, glass só na direita, sem wrapper/borda redundante).
- **Layout de login unificado:** criado `frontend/src/components/login/LoginCardLayout.jsx` como fonte única do layout do card — antes a tela pública e o preview administrativo eram duas árvores JSX paralelas, causa raiz de divergência visual recorrente.
- **Workflow de deploy passou a detectar escopo:** job `changes` compara os arquivos alterados no push e decide separadamente se publica `worker`, `frontend` ou os dois — mudança só num lado para de republicar o outro à toa.
- **Geometria da borda pulsante sincronizada com o `radius` real do card** (antes usava valor fixo, causava "ponta" visual nos cantos); ajuste fino subsequente no piso de `roundness` do shader.
- **Imagem de fundo dedicada pro login** (`login_background_image_url`, migration `0005_login-background.sql`) — independente da imagem institucional (`login_image_url`).
- **Actions do GitHub atualizadas pra v5** (`checkout`/`setup-node`), antecipando a deprecação do runtime `node20` anunciada pelo GitHub.
- **Armadilha de ambiente — ACL quebrada entre Codex e Claude Code no Windows:** Claude Code recebeu erro de permissão em caminhos específicos (`worker/migrations/`, dois arquivos de branding/login, o workflow). Causa: Codex roda sob identidade Windows sandbox própria e, ao recriar um caminho, às vezes não herda a ACL corretamente. A permissão de escrita compartilhada (`CodexSandboxUsers`, `Modify`) é uma entrada **explícita da raiz do repositório**, não herdada de pastas acima. **Correção sempre no caminho específico afetado** (`takeown /F <caminho> /R /D Y` + `icacls <caminho> /reset /T`) — nunca resetar a raiz do projeto, ou a entrada de acesso do Codex ao repo inteiro some.

### 2026-07-13

- **Primeira rodada de hardening de segurança**, a partir de uma auditoria externa: IDOR corrigido em `PATCH /api/notifications/:id/read` (passou a filtrar por `agent_id` do token); `verifyPassword` tornado timing-safe (PBKDF2 completo mesmo com e-mail inexistente + comparação byte a byte em tempo constante); rate limit de login passou a valer por IP **e** por e-mail; segunda camada opcional (`SETUP_TOKEN`) em `POST /api/setup/admin`; `secureHeaders` no worker e CSP/headers no frontend; `npm audit` não-bloqueante adicionado ao CI. **Decisão registrada nesta data: migração de sessão pra cookie `HttpOnly` foi deliberadamente adiada** — front e worker em domínios diferentes exigiriam `SameSite=None`, considerado na época risco maior que o benefício imediato. (Fechada só em `2026-07-22`.)
- **Limpeza de features órfãs:** `password_reset_tokens`/`RESEND_API_KEY` removidos — nunca tiveram rota funcional, confirmado `0` linhas em produção antes de dropar.
- **Wrangler atualizado pra `^4.0.0`** nos dois pacotes (alinhado ao CI), dependência morta `jose` removida (JWT sempre foi hand-rolled).
- **Runbook único de migration criado** (`worker/scripts/run-migrations.js`, `db:migrate`/`db:migrate:remote`) — motivado por um incidente real de migration aplicada parcialmente contra produção sem erro reportado. **Regra dura a partir daqui: depois de qualquer migration multi-instrução via `--remote`, confirmar o resultado real via `sqlite_master`, nunca confiar só no exit code.**
- **Borda CSS duplicada removida** do card de login (dois elementos desenhando contorno no mesmo raio, causava costura visual nos cantos).
- **`fetch-depth: 2` insuficiente** no job de detecção de escopo — um push agrupando múltiplos commits quebrava o `git diff`. Trocado pra `fetch-depth: 0` nesse job específico.
- **Selo "Acesso institucional" removido** da coluna esquerda do login.

### 2026-07-14

- **Deduplicação de código:** lógica de exibição de contato (ícone/cor/label, badge de urgência), antes copiada entre `PatientDetail.jsx` e `PatientPanel.jsx`, extraída pra `frontend/src/utils/contactDisplay.js`. Helpers de mistura de cor duplicados entre `visualThemes.js`/`darkPalette.js` extraídos pra `theme/colorUtils.js`. Defaults de branding duplicados entre store e formulário admin, unificados. Função morta `SettingsTab()` removida de `Admin.jsx`.
- **Testes automatizados criados do zero:** `vitest` no frontend (cobertura inicial `32/32`), suíte do worker expandida além de protocolos (`25/25`).
- **Coluna legada `patients.protocol_days` removida** (migration `0009`), depois de confirmar `0` pacientes dependendo do fallback `LEGACY` no D1 remoto.
- **Incidente de lockfile no CI:** `vitest@4.1.10` puxou um `esbuild` isolado incompatível com o `vite@5` do projeto — `npm ci` do CI (Linux) falhava mesmo com `npm install` local (Windows) aparentando estar ok. Corrigido fixando `vitest@^3.2.4`, depois `3.2.7` por uma CVE crítica encontrada na 3.2.4/3.2.5 via `npm audit`. **Prática consolidada: depois de mexer em dependência dev, rodar `rm -rf node_modules && npm ci` local antes de subir — replica o passo exato do CI.**
- **Node do job de deploy do worker subido pra `22`** (exigência do `wrangler@4.110`, efeito colateral direto de ter resolvido `^4.0.0` pra última 4.x disponível).
- **Protocolo de Documentos — início da implementação** (feature nova, concluída em `2026-07-20`): catálogo administrável (`document_templates`) + checklist por paciente (`patient_documents`), migration `0011_document-protocol.sql`.

### 2026-07-20

- **Bug reportado pelo usuário:** timeline aparecendo no detalhe do paciente mesmo sem nenhum protocolo de contato cadastrado. Investigação (só leitura, D1 remoto): `contact_protocols` com `0` linhas — o culpado era `app_settings.contact_protocol_days`, resíduo do antigo fallback `GLOBAL`, ainda lido por `resolvePatientProtocol()`.
- **Decisão do usuário:** não só limpar o valor residual — remover o mecanismo de fallback inteiro. `resolvePatientProtocol()` simplificado de `LINKED → DEFAULT → GLOBAL → LEGACY` pra só **`LINKED`/`EMPTY`**. Rota `PATCH /api/settings/protocol` removida (não fazia mais sentido sem nível `GLOBAL`). Migration `0012_remove-global-protocol-fallback.sql` apaga a chave residual. `is_default` em `contact_protocols` mantido, mas só pra pré-selecionar protocolo no cadastro — deixou de ser fallback de leitura.
- **Protocolo de Documentos concluído:** decisão inicial era atribuir documento só na página do paciente; revertida depois do usuário ver a tela `Novo Paciente` publicada e pedir a seleção também ali. Exclusão de template em uso bloqueada com `409` (mesmo padrão de `contact_protocols`).
- **Auditoria de escalabilidade feita com o usuário** — seis frentes avaliadas, decisão item a item:
  - Multi-tenancy: **fora**, revisitar só depois do sistema pronto pra dado real.
  - Auditoria/LGPD completa: **aprovada em versão enxuta** → virou a aba de Histórico.
  - Busca real (FTS5) + paginação cursor-based: **aprovada só a indexação/paginação** (junto do Histórico); FTS5 fica pra quando a base crescer de verdade.
  - Observabilidade/log de erro: **aprovada** → aba de Logs, ainda não implementada.
  - Backup automatizado + restore testado: **fora por enquanto** — só dado de teste hoje.
  - Cache de catálogo read-heavy: **aprovada**, ainda não implementada.
  - Ordem de execução definida: Histórico (risco baixo, leitura pura) → Logs (novo caminho de escrita) → Cache (otimização por cima do que já funciona). Prioridade oficial do projeto: **segurança > saúde do banco > fluidez**.

### 2026-07-21

- **Aba de Histórico implementada e publicada** (primeira frente do roadmap de escalabilidade): `GET /api/activity`, paginado, `UNION ALL` de pacientes cadastrados + contatos realizados. Migration `0013_activity-history.sql` — `patients.created_by` (decisão confirmada pelo usuário: registrar quem cadastrou) + dois índices novos (`idx_followups_created`, `idx_patients_created`). Página própria no menu lateral (`Historico.jsx`), decisão confirmada pelo usuário. Deploy feito depois de bateria de testes verde (worker `29/29`, frontend `32/32`) e autorização explícita.

### 2026-07-22

- **Segunda rodada de hardening de segurança**, disparada por um pentest externo (`SECURITY_IMPROVEMENTS.md`) contra o ambiente publicado. Escopo definido pelo usuário: troca de senha do admin default já feita por ele antes desta rodada; 2FA (TOTP) e verificação por e-mail **recusados explicitamente**; 2FA da conta Cloudflare em si fica por conta dele, fora do código.
- **Primeira leva (commit `feb5970`):**
  - Sanitização server-side de paciente (`worker/src/routes/patients.js`): `stripHtml` remove tag de `name`/`procedure`/`notes`, limite de tamanho, `surgery_date` validado por regex, `status` validado contra enum, `assigned_agent_id` validado como FK real (mesmo padrão que já existia pra `protocol_id`).
  - Header `Retry-After` no `429` de rate limit, calculado a partir do `locked_until` real.
  - Login: removido o botão de mostrar/ocultar senha (pedido literal do usuário — não deixar resíduo de senha visível no inspecionar; campo agora é sempre `type="password"` e o valor é limpo do estado após erro de login).
  - CSP de produção: removido resíduo `http://localhost:8787` do `connect-src`.
  - Simulação de ataque rodada contra produção antes do deploy (SQLi/NoSQL, `alg:none`, cookie forjado, CORS malicioso, path traversal, `TRACE`) — nenhum vetor comprometeu nada; achado real foi que produção ainda rodava o código antigo (confirmou que o fix não tinha sido publicado ainda).
- **Segunda leva (commit `3f1929d`) — migração de sessão pra cookie `HttpOnly`:** fecha a decisão deixada deliberadamente pendente em `2026-07-13`.
  - `worker/src/middleware/auth.js`: `signToken` (TTL fixo `8h`) virou `signAccessToken` (**`15min`**) + `signRefreshToken` novo (**`7 dias`**, claim `type:'refresh'`). `authMiddleware` lê o cookie `access_token` primeiro, com fallback pro header `Authorization: Bearer` (transição). `readCookie()` novo — parser de cookie feito a mão, mesmo estilo hand-rolled do JWT no arquivo.
  - `worker/src/routes/auth.js`: login seta os dois cookies; novos `POST /auth/refresh` (relê o agente no banco antes de emitir novo access token — mudança de `role`/desativação vale sem esperar o refresh expirar) e `POST /auth/logout` (limpa os dois cookies, idempotente).
  - **Atributos do cookie — desvio deliberado da recomendação do guia externo:** `SameSite=None` (não `Strict`) + `Secure` + `HttpOnly`. Motivo: `pages.dev` e `workers.dev` são domínios (sites) diferentes — `Strict`/`Lax` bloqueariam o navegador de mandar o cookie em qualquer request cross-site, quebrando o login inteiro, silenciosamente. `Secure` funciona em `http://localhost` porque Chrome/Firefox tratam `localhost` como origem confiável mesmo sem HTTPS.
  - Frontend: `token` saiu inteiramente da store (`store/index.js` só persiste `agent`); `services/api.js` manda `credentials:'include'` em toda chamada e, em qualquer `401` fora de `/auth/login`/`/auth/refresh`, tenta renovar a sessão uma vez antes de deslogar; botão "Sair" (`AppLayout.jsx`) agora chama `api.auth.logout()` no servidor antes de limpar o estado local.
  - Novo secret `JWT_REFRESH_SECRET` (local: `.dev.vars`; produção: `wrangler secret put`, configurado antes do deploy do worker).
  - Testes: `worker/test/auth-middleware.test.js` atualizado (`signToken`→`signAccessToken`, TTL `8h`→`15min`, teste de auth via cookie + `readCookie`) — `31/31`.
- **Armadilha encontrada na validação local:** D1 local (`.wrangler/state`) é banco **completamente separado** do D1 remoto — a senha de admin trocada em produção não existe localmente; o admin local seed tem hash `$PLACEHOLDER_HASH$` (proposital, nunca bate com senha nenhuma). A correção natural (`node worker/scripts/create-admin.js` → `POST /api/setup/admin`) **também falhou**, porque `APP_ENV="production"` em `[vars]` do `wrangler.toml` vale igual sob `wrangler dev --local` (sem override de ambiente) — `/api/setup` fica bloqueado localmente também, não só em produção. Contornado calculando o hash PBKDF2 a mão (mesmo algoritmo do `hashPassword()`, via Web Crypto do Node) e gravando direto no D1 local via `wrangler d1 execute --local`. Usuário validou o login local com sucesso depois disso.
- **Deploy:** `JWT_REFRESH_SECRET` de produção gerado e configurado via `wrangler secret put` antes do deploy do worker; commits `feb5970`/`3f1929d` — push e deploy manual (`deploy-worker.ps1` + `deploy-frontend.ps1` — `deploy-all.ps1` pede confirmação interativa, incompatível com shell não-interativo). Persistência de configuração visual checada explicitamente a pedido do usuário (`GET /api/settings/public`) — intacta; deploy de worker não roda migration nem toca em dado.
- **Segunda simulação de ataque + auditoria de saúde do banco, pós-deploy (pedido explícito do usuário):**
  - Login/API: SQLi clássico/`UNION`/time-based blind seguros; XSS refletido inerte; `alg:none` rejeitado via header e via cookie forjado; `/auth/refresh` rejeita cookie ausente ou do tipo errado; CORS preflight de origem não-whitelisted sem `Access-Control-Allow-Origin`; rate limit + `Retry-After` confirmados ao vivo.
  - CSRF (raciocínio dado `SameSite=None`): não explorável — CORS não reflete origem arbitrária e os endpoints exigem `Content-Type: application/json`, que um `<form>` simples não manda sem JS.
  - `logout` sem sessão limpa os dois cookies mesmo assim; `change-password` sem token → `401`; `/api/setup` continua bloqueado em produção; CSP/HSTS/`X-Frame-Options`/`nosniff` intactos.
  - Banco (D1 remoto, só leitura): zero `patients.protocol_id`/`assigned_agent_id` órfão; zero conta com hash placeholder em produção; os `14` índices esperados presentes via `sqlite_master`. Contagem de linhas nessa data: `agents=1`, `patients=3`, `contact_protocols=0`, `followup_logs=0`, `notifications=4`, `app_settings=35`, `document_templates=3`, `patient_documents=6`, `login_rate_limit=17`.
- **Itens do guia externo que ficaram fora desta rodada** (decisão consciente, não esquecimento): CAPTCHA/Turnstile após tentativas repetidas, criptografia em nível de aplicação pra coluna sensível + base legal/retenção LGPD formal, crack offline do secret (informativo).

### 2026-07-23

- **Este documento reescrito, duas vezes.** Primeira reescrita separou "estado atual" (seções por tópico) de "changelog" — mas isso recriou o mesmo problema que motivou a reescrita: uma data de conclusão de roadmap (`2026-07-21`) ficava numa seção separada e parecia "desatualizada" perto do resto. **Segunda reescrita (esta): tudo numa única linha do tempo, ordem cronológica estrita, sem seção de estado paralela com data própria.** Regra daqui pra frente: qualquer atualização deste arquivo vira uma nova entrada no fim desta seção, na data do dia; nunca editar uma seção "estado atual" desconectada da linha do tempo.

### 2026-07-24

- **`CLAUDE.md` criado na raiz.** Diretrizes de trabalho que antes precisavam ser reativadas manualmente a cada sessão (padrão de comunicação, regra de quando parar e perguntar, padrão de código sênior humanizado, regras duras herdadas de incidente real). Escolhido `CLAUDE.md` em vez de hook `SessionStart` porque carrega automaticamente sem custar chamada extra de API. O arquivo aponta pra `Status.md`/`ARQUITETURA.md`/`README.md` em vez de duplicar contexto.
- **Aba de Logs implementada** (segunda frente do roadmap de escalabilidade de `2026-07-20`, depois do Histórico):
  - **Escopo decidido com o usuário: apenas erro de servidor** (`500`/exceção não tratada). `4xx` e evento de segurança (login falho, `403`, `429`) ficaram **fora de propósito** — incluir evento de segurança criaria escrita em fluxo normal, e um ataque de força bruta viraria escrita em rajada no D1.
  - Tabela `error_logs` (migration `0014_error-logs.sql`) + índice `idx_error_logs_occurred`. `agent_id`/`agent_email` são **snapshot sem FK, de propósito**: uma FK com `ON DELETE SET NULL` apagaria a autoria do erro justamente ao desligar o agente, que é quando a informação mais importa.
  - Escrita em `worker/src/services/error-log.js`, chamada do `app.onError`. `recordServerError` **nunca rejeita** — falha de gravação dentro do error handler mascararia o erro original. Gravação passa por `c.executionCtx.waitUntil` pra não atrasar a resposta `500`; `try/catch` em volta porque `executionCtx` não existe fora do runtime do Workers (teste/invocação direta).
  - **Retenção de 30 dias**, limpeza pegando carona no `runScheduler` diário (`purgeOldErrorLogs`) — sem isso a tabela cresce indefinidamente. Falha na limpeza não invalida as notificações já criadas na mesma execução.
  - `GET /api/logs` paginado (`{ items, total, retention_days }`), `authMiddleware` + `adminOnly` — `message`/`stack` expõem detalhe interno da aplicação. Página `Logs.jsx` com stack recolhido sob demanda, rota `adminOnly` e item de menu só pra admin.
  - **Deduplicação aproveitada no caminho:** `parseTs` (normalização de `datetime('now')` do SQLite, que vem UTC sem sufixo) saiu de dentro de `Historico.jsx` pra `frontend/src/utils/sqliteDate.js`, agora usada pelas duas páginas. Bloco de `NavLink` do menu, que estava copiado entre a lista comum e o item de admin, virou `ADMIN_NAV_ITEMS` + `navLinkClass` em `AppLayout.jsx`.
  - `worker/src/db/schema.sql` atualizado junto (tabela + índice), mantendo o schema base em dia com as migrations.
  - Validação: worker `36/36` (5 testes novos em `test/error-log.test.js`, com D1 falso), frontend `32/32`, build do frontend ok (alerta de bundle > 500kB é o pré-existente do shader).
- **Publicação da aba de Logs.** Ordem seguida de propósito: **migration remota antes do deploy** — o worker novo lendo `error_logs` inexistente devolveria `500` em `/api/logs`. `0014` aplicada no D1 remoto e confirmada via `sqlite_master` (tabela + `idx_error_logs_occurred`), depois `git push` na `main` deixando o GitHub Actions publicar worker e frontend (run verde, escopo detectou os dois lados). Pós-deploy: `/api/logs` sem sessão devolve `401` (rota registrada, `adminOnly` ativo) e `/health` `200`.
- **Fluxo "esqueci minha senha" implementado** (código de 6 dígitos por e-mail), fechando o gargalo registrado logo abaixo. Ainda **não publicado** — depende de um passo manual do usuário (publicar o Apps Script).
  - **Por que não é SMTP:** Cloudflare Workers não abre conexão SMTP (bloqueio antispam da própria Cloudflare), então **senha de app do Gmail não serve** — ela só funciona para SMTP/IMAP. Decisão do usuário depois dessa constatação: **Google Apps Script publicado como Web App** faz a ponte. Worker manda `POST` HTTP, o script chama `MailApp.sendEmail()`, e o remetente é o Gmail da clínica. Custo zero, dentro da conta Google que já existe.
  - Volume esperado confirmado pelo usuário: ~1 e-mail/dia, no máximo 5/dia mesmo escalando — folgado dentro dos ~100/dia da conta Gmail gratuita. Cota não foi tratada em código de propósito.
  - Tabela `password_reset_codes` (migration `0015`). Guarda **só o SHA-256 do código**. Ressuscita a ideia do `password_reset_tokens` removido em `2026-07-13`, desta vez nascendo junto das rotas que a usam.
  - `POST /auth/forgot-password` responde **sempre o mesmo corpo**, com ou sem conta — diferença de mensagem, status ou tempo viraria oráculo de enumeração de contas. O rate limit conta o pedido mesmo sem conta existente, senão o próprio limite viraria o detector. Único desvio proposital: `429` real com `Retry-After`, porque sem ele o envio de e-mail vira a arma (flood na caixa da vítima + estouro de cota).
  - `POST /auth/reset-password` valida código, expiração (15 min), uso único e limite de 5 tentativas **por código** — é a defesa principal, já que 6 dígitos é pouca entropia. Comparação em tempo constante. Pedir código novo aposenta o anterior. Ao concluir, limpa o rate limit de login do e-mail (senão a pessoa redefinia a senha e ainda pegava `429`).
  - Transporte isolado em `worker/src/services/email.js` — trocar por Resend/Gmail API depois é reescrever esse arquivo, sem tocar no fluxo. Secrets novos: `EMAIL_RELAY_URL`, `EMAIL_RELAY_TOKEN`.
  - Frontend: link "Esqueci minha senha" no login, página `/esqueci-senha` em duas etapas (e-mail → código + senha nova). A casca visual das telas públicas saiu de dentro de `Login.jsx` pra `components/LoginPageShell.jsx` — mesma motivação do `LoginCardLayout` em `2026-07-12`: sem isso, toda tela pública nova nasce como árvore JSX paralela e diverge no primeiro ajuste de branding.
  - **Armadilha de ACL de novo** (mesma família de `2026-07-12`): `frontend/src/components/login/` está com permissão negada pra criar arquivo novo, e `takeown`/`icacls` falharam por falta de shell elevado. Por isso `LoginPageShell.jsx` ficou em `components/` em vez de `components/login/`. Marcado como `LIMITACAO:` no próprio arquivo.
  - Validação: worker `44/44` (8 testes novos), frontend `32/32`, build ok. Migration aplicada no D1 local e confirmada via `sqlite_master`. Fluxo exercitado ponta a ponta contra `wrangler dev`: resposta genérica pra conta inexistente, código correto redefine (`200`), reuso do mesmo código é rejeitado, e login com a senha nova funciona.
  - **Documentação da publicação:** `docs/EMAIL-APPS-SCRIPT.md` + script pronto em `docs/apps-script-email.gs`.
- **Aba "Mensageria" em Configurações.** Decisão do usuário: configurar as credenciais de e-mail pela própria interface, em vez de `wrangler secret put`. Chaves em `app_settings`: `email_enabled`, `email_relay_url`, `email_relay_token`, `email_from_name`.
  - **Trade-off assumido e mitigado:** `app_settings` é texto claro no D1, diferente de um Worker Secret. Mitigações: o token **nunca sai em claro numa leitura** (`GET /api/settings` devolve mascarado, só os 4 últimos caracteres — e essa rota vale pra qualquer agente autenticado, não só admin); não entra no payload de `/settings/public` (que já é allowlist); e o `PATCH` **ignora valor mascarado**, senão salvar o formulário sem tocar no campo apagaria a credencial.
  - `resolveEmailConfig()` dá prioridade ao que foi salvo no painel e mantém a variável de ambiente como fallback, pra não quebrar quem tinha secret configurado antes da aba existir.
  - `POST /api/settings/email/test` (admin) manda um e-mail de teste pro próprio admin logado e devolve a cota restante do dia. Existe porque erro de configuração de relay só apareceria quando alguém realmente precisasse do reset — a pior hora pra descobrir. A mensagem de erro real do relay volta pro admin de propósito: sem ela, distinguir token errado de URL errada de cota estourada vira adivinhação.
  - Validação: worker `57/57` (7 testes novos), frontend `32/32`, build ok. Vazamento do token verificado ao vivo contra `wrangler dev` — `GET /api/settings` autenticado devolveu `email_relay_token: "••••••••9876"`, com o valor real gravado no banco.
- **E-mail da conta admin trocado de `admin` para `[e-mail removido]`** direto no D1 remoto, a pedido do usuário. **Isso muda o login** — a coluna `email` de `agents` é a própria credencial. Senha inalterada. Destrava o fluxo de reset por e-mail, que antes não tinha para onde enviar.
- **Campos de contato opcionais** (migration `0016`): `patients.email` e `agents.phone`. Decisões do usuário: os dois **não obrigatórios**, e o `patients.phone` que já existia **fica como está** (rótulo "Telefone") em vez de virar "Celular" ou ganhar um segundo campo. O e-mail do agente não entra aqui — já existe e é a credencial de login, com regra própria de unicidade.
  - Sanitização compartilhada em `worker/src/utils/contactFields.js`: vazio e ausente viram `null` (string vazia no banco quebraria `WHERE ... IS NOT NULL` depois), telefone aceita só `dígito + ( ) espaço -`, e-mail é normalizado pra minúsculo e validado por **formato plausível, não estrito** — aqui é campo de contato, nunca credencial nem destino de envio automático, e regra dura rejeitaria endereço válido.
  - Busca de pacientes passou a cobrir `email` junto de nome e telefone.
  - Frontend: campo no cadastro e na edição de paciente, exibição com `mailto:` no detalhe, campo "Celular" no formulário de agente e na lista de equipe.
  - Validação: worker `50/50` (6 testes novos), frontend `32/32`, build ok, migration aplicada no D1 local.
- **Senha do admin (`admin`, única conta do sistema) redefinida em produção,** a pedido do usuário. Como não existe rota de reset por e-mail (o fluxo foi removido em `2026-07-13`), o caminho foi gerar o hash PBKDF2 fora do worker com **exatamente o mesmo algoritmo do `hashPassword()`** (SHA-256, 100k iterações, 256 bits, salt de 16 bytes, serializado como `{"salt":[...],"hash":[...]}`) e gravar via `wrangler d1 execute --remote`. Confirmado com login real contra produção (`200`) — o `changes: 2` reportado pelo D1 no `UPDATE` é contabilidade interna dele, não duas contas: a tabela tem `1` agente e `1` hash. Script gerador e arquivos temporários com a senha foram apagados depois. **Ponto de atenção que segue aberto: trocar senha exige acesso ao D1 de produção.** Enquanto não houver fluxo de reset no produto, isso é um gargalo operacional real. (Fechado em `2026-07-25`, com a mensageria funcionando.)

### 2026-07-25

- **Mensageria configurada e funcionando em produção.** Apps Script publicado na conta Google da clínica, credenciais salvas pela aba do painel, envio confirmado pelo usuário. Isso fecha o gargalo de reset de senha aberto em `2026-07-24`.
  - **Armadilha do Apps Script que custou algumas rodadas de diagnóstico:** salvar o código no editor **não** atualiza a implantação — ela continua servindo a versão congelada no momento em que foi publicada. Sintoma: o relay respondia HTML do Google terminando em `Função de script não encontrada: doPost`, que numa leitura rápida parece erro de permissão (o corpo é uma página inteira do Google, e a mensagem real fica no fim). **Depois de editar o script, sempre `Implementar → Gerir implementações → ✏️ → Versão: Nova versão`.** Criar implantação nova em vez de versão nova gera URL diferente e exige atualizar o painel.
  - Outra pegadinha: a propriedade `RELAY_TOKEN` foi preenchida com o ID do script — os dois campos ficam na mesma tela de Definições, um logo acima do outro.
  - **`curl -L` não serve pra testar o relay:** o Apps Script redireciona pra `googleusercontent.com` e o `curl` perde o `Content-Length` no salto, devolvendo `411`. `Invoke-RestMethod` do PowerShell atravessa certo.
- **Auditoria de segurança das credenciais de e-mail** (pedido do usuário depois do envio funcionar). Verificado ao vivo contra produção:
  - Token nunca sai em claro da API (`••••••••f07f`); token e URL não aparecem no histórico do git, em arquivo versionado nem no bundle publicado do frontend (o único casamento no bundle era o `placeholder` do campo do formulário); `/api/settings/public` não expõe nada de mensageria; `/api/settings/email/test` sem sessão dá `401`.
  - **Teste mais relevante: o relay foi chamado direto com token errado e sem token — `{"ok":false,"error":"unauthorized"}` nos dois casos.** A URL sozinha não envia nada, então descobri-la não dá a ninguém acesso à conta Gmail.
  - **Achado corrigido:** `GET /api/settings` exige login mas não exigia admin, e devolvia `email_relay_url` em claro pra qualquer agente autenticado. Inócuo hoje (só existe a conta admin), mas viraria exposição real assim que a clínica cadastrasse agentes. `redactSettings()` passou a receber o papel: **agente comum não recebe nenhuma chave de mensageria; admin recebe com o token mascarado.** Default sem parâmetro é o modo restrito, pra que esquecer de passar o papel não vaze.
  - **Risco aceito e registrado:** o token continua em texto claro no D1. Quem tiver acesso ao banco (via dashboard ou API token da Cloudflare) consegue lê-lo. Mitigar exigiria criptografar com uma chave em Worker Secret — descartado por ora, porque reintroduz a dependência de `wrangler secret` que motivou a criação da aba.
  - Validação: worker `59/59` (2 testes novos cobrindo o filtro por papel, incluindo o default restrito).
  - **PENDENTE DE PUBLICAÇÃO (decisão do usuário: deixar assim por enquanto).** O commit `5cdcab9` está na `main` local mas **não foi deployado** — produção ainda devolve `email_relay_url` em claro pra qualquer agente autenticado. Sem impacto hoje, porque a conta admin é a única do sistema. **Vira exposição real no momento em que o primeiro agente for cadastrado — publicar antes disso.**
- **Código de redefinição não chegava — não era bug, era o e-mail cadastrado.** `password_reset_codes` estava vazia (nenhum código gerado) e o rate limit registrava a chave `reset:email:[e-mail removido]`, enquanto a conta estava com `[e-mail removido]`. **A conta real do usuário é `[e-mail removido]`** — o endereço sem `gf` foi passado por engano em `2026-07-24` e a divergência, apontada duas vezes ao ver a conta Google numa screenshot, passou batido no meio de outros ajustes. Cadastro corrigido no D1 remoto; **login do admin agora é `[e-mail removido]`**.
  - **Efeito colateral que só apareceu aqui:** os e-mails de teste da aba Mensageria estavam saindo para `[e-mail removido]`, endereço de terceiro. O botão reportava sucesso porque o envio de fato ocorria — só não para a caixa do usuário. Lição: "o teste passou" não é o mesmo que "a mensagem chegou".
  - Validado ao vivo depois da correção: `forgot-password` gerou o código e disparou o e-mail, sem precisar de deploy — o fluxo já estava publicado desde `2026-07-24`.
- **Códigos de reset deixam de acumular** (pedido do usuário). Antes o código antigo era marcado com `used_at` e ficava no banco; agora é `DELETE`, tanto ao pedir um código novo quanto ao concluir a redefinição. **A tabela passa a ter no máximo uma linha por agente.**
- **Faxina noturna às 00h de Fortaleza** (pedido do usuário). Segundo cron adicionado (`0 3 * * *` em UTC — Fortaleza é UTC-3 o ano inteiro, sem horário de verão), roteado em `index.js` pelo valor de `event.cron`. `runNightlyCleanup()` limpa `error_logs` (mantendo a retenção de 30 dias) e `password_reset_codes` (só o que já expirou — o filtro existe pra nunca derrubar um pedido em curso na hora da faxina). Cada limpeza é isolada: uma falhar não impede a outra. A limpeza de logs saiu de dentro de `runScheduler()`, que volta a ter uma responsabilidade só.
  - **Nota sobre os logs:** eles continuam com retenção de 30 dias, não zerados toda noite — zerar diariamente esvaziaria a aba de Logs e tiraria a utilidade dela. Se a intenção for outra, é um ajuste de uma linha.
  - Validação: worker `60/60` (1 teste novo).
- **Modelos de e-mail editáveis + resumo diário do agente** (migration `0017`). O envio deixa de servir só à recuperação de senha.
  - **Decisões do usuário nesta rodada:** corpo em **HTML livre** numa textarea (em vez de layout fixo); desempenho do dia e agenda de amanhã **num único e-mail às 20h** (em vez de dois envios); **sem consolidado da equipe pro admin** por enquanto — a aba Histórico já cobre.
  - Tabela `email_templates`, uma linha por tipo (`password_reset`, `daily_digest`), com `subject`, `body_html` e `is_enabled`. Tabela própria em vez de `app_settings` porque corpo de e-mail é texto longo. Sem versionamento: catálogo de versões que ninguém consulta só acumula linha.
  - **Reusa `renderMessageTemplate` do Protocolo de Mensagens** (sintaxe `{{chave}}`) em vez de criar um segundo motor de template no projeto.
  - **Regra de escape que vale entender antes de mexer:** o corpo é HTML escrito pelo admin, logo confiável; os **valores** interpolados vêm do banco e são escapados. A exceção é `tomorrow_list`, que o próprio sistema monta como HTML — ali o escape é feito campo a campo dentro de `daily-digest.js`. O assunto é desescapado no fim, porque é texto puro e `&amp;` apareceria literal pra quem lê.
  - `<script>` é recusado no salvamento. Não roda em cliente de e-mail de qualquer forma; a barreira existe pro preview do painel, que ainda assim renderiza em `<iframe sandbox="">` sem `allow-scripts`.
  - **Terceiro cron:** `0 23 * * *` (20h Fortaleza). `index.js` passou a rotear por um mapa de cron; cron desconhecido cai no scheduler de follow-ups, que era a rotina única antes. Os três horários: `0 11` follow-ups, `0 23` resumo, `0 3` faxina.
  - `daily_digest` reusa `getNextPendingMilestone`/`resolvePatientProtocol` — mesma resolução de protocolo do scheduler, sem duplicar a regra. Desempenho sai de `followup_logs` agrupado por `outcome`, filtrando por `contact_date` (a data que o agente informou) e não por `created_at`.
  - **ASSUME fuso America/Fortaleza (UTC-3, sem horário de verão)** no cálculo de "hoje"/"amanhã": às 20h local já é o dia seguinte em UTC, então usar UTC direto erraria a data.
  - Agente sem e-mail válido é pulado com aviso no log — a coluna `email` é o login e pode não ser endereço real. Falha no envio de um agente não interrompe os demais.
  - `POST /api/settings/email/test-digest` manda o resumo real pro admin logado; sem isso, conferir uma edição de template exigiria esperar as 20h.
  - Validação: worker `70/70` (10 testes novos), frontend `32/32`, build ok, migration aplicada no D1 local e rota exercitada contra `wrangler dev`.
  - **Rotação do token, se um dia for desejada:** o token em uso foi gerado durante a sessão de trabalho e apareceu em texto no transcript, que fica em disco nesta máquina. Não é vazamento (máquina do próprio dono), mas rotacionar é barato: gerar outro valor, trocar na propriedade `RELAY_TOKEN` do Apps Script **e** no campo da aba Mensageria, e republicar o script como versão nova.
