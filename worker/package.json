{
  "name": "caredesk-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute caredesk --local --file=src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute caredesk --file=src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "jose": "^5.6.3"
  },
  "devDependencies": {
    "wrangler": "^3.65.0"
  }
}
